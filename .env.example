# RAG Reference Architecture - Environment Variables
# Copy this file to .env and fill in your values
# cp .env.example .env

# =============================================================================
# CLOUDFLARE TUNNEL
# =============================================================================

# Tunnel token from Cloudflare Zero Trust dashboard
# Get this from: Zero Trust -> Networks -> Tunnels -> Your Tunnel -> Configure
# Format: eyJhIjo... (base64 encoded JSON)
TUNNEL_TOKEN=your_tunnel_token_here

# =============================================================================
# OPTIONAL: ELASTICSEARCH
# =============================================================================

# Elasticsearch heap size (default: 4g)
# ES_JAVA_OPTS="-Xms4g -Xmx4g"

# =============================================================================
# OPENWEBUI
# =============================================================================

# OpenWebUI secret key - REQUIRED for MCP tools to work after restart
# Generate with: openssl rand -hex 32
WEBUI_SECRET_KEY=your_secret_key_here

# =============================================================================
# MCP TOOLS (OPTIONAL)
# =============================================================================

# GitHub personal access token for GitHub MCP server
# Create at: https://github.com/settings/tokens
# Required scopes: public_repo (for read-only public repo access)
# Leave empty to disable GitHub MCP
GITHUB_TOKEN=

# =============================================================================
# N8N WORKFLOW AUTOMATION
# =============================================================================

# n8n public URL for webhooks (e.g., https://n8n.your-domain.com)
# Required for webhooks to work via Cloudflare Tunnel
N8N_WEBHOOK_URL=https://n8n.your-domain.com

# n8n hostname (without protocol)
N8N_HOST=n8n.your-domain.com

# n8n editor base URL (same as webhook URL for Cloudflare setup)
N8N_EDITOR_BASE_URL=https://n8n.your-domain.com
